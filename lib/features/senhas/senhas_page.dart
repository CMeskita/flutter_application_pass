import 'package:flutter/material.dart';

class SenhasPage extends StatelessWidget {
  const SenhasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
