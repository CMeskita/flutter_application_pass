import 'package:flutter/material.dart';

class SplasController extends StatelessWidget {
  const SplasController({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
